@model ItechArt.Survey.WebApp.ViewModels.LoginViewModel

@{
    Layout = "_Layout";
}

<form asp-antiforgery="true" asp-action="Login" asp-controller="Account">
    <div class="container d-flex justify-content-center">
        <div class="table w-50">
            <h1>Sign in</h1>
            <p>Please fill in this form to login your account.</p>
            <hr>
            <div class ="pb-2">
                <caption asp-for="UserName">Username</caption>
                <input type="text" class="form-control w-50" asp-for="UserName" placeholder="Enter username"/>
                <span class="text-danger small" asp-validation-for="UserName"></span>
            </div>
            <div class ="pb-2">
                <caption asp-for="Password" text="Password">Password</caption>
                <input type="password" class="form-control w-50" asp-for="Password" placeholder="Enter password"/>
                <span class="text-danger small" asp-validation-for="Password"></span>
            </div>
            <input type="submit" value="Sign in" class="btn btn-outline-dark"/>
            <div class="text-danger small" asp-validation-summary="ModelOnly"></div>
        </div>
    </div>
</form>

@section Script {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}